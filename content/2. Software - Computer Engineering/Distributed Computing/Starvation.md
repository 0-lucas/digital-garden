# What is it?

In [[Multi-Threading]] applications and [[Distributed Computing]] systems, *starvation* occurs when **high priority [[Threads|threads]] keep executing and low priority [[Threads|threads]] are blocked indefinitely**. This way, the low priority [[Threads|thread]] will never get any *CPU time*.

![[starvation.png]]