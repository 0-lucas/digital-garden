# What is it?

A *decentralized* [[Architecture Patterns|architecture]] refers systems which control and data are **distributed across nodes**, not controlled from a single master node, which increases **expansion potential**, **[[Load Balancing]]** and **error resilience**.
Applications like *blockchain* and *Peer-to-peer networks* work as *decentralized* *architectures*.
